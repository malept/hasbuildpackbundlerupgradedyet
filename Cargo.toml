[package]
name = "hasbuildpackbundlerupgradedyet"
version = "0.1.2"
authors = ["Mark Lee"]
license = "AGPL-3.0"

[features]

lint = ["clippy"]

[dependencies]

env_logger = "0.3"
hyper = "0.10   "
log = "0.3"
redis = "0.5"
regex = "0.1"
reqwest = "0.3"
rquery = "0.2"
semver = "0.2"
serde = "0.8"
serde_json = "0.8"

clippy = { version = "*", optional = true }
